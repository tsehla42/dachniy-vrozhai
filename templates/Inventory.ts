import type { CategoryTemplateCollection } from '~/utils/types/CategoryTypes';

export const INVENTORY_TEMPLATE: CategoryTemplateCollection = [['Лопати'], ['Граблі'], ['Вила'], ['Тяпки'], ['Шланги']];
